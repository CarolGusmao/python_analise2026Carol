v = float(input("Olá funcionário! Digite seu salário em reais :"))
if v >5000 and v <= 8000:
        irrf= (v * 7.5) / 100
        sf= v-irff
        print (f' O valor do salário é {v}. O valor do imposto a pagar é {irrf}. Salário final é {sf}' )
elif  v  <=5000 :
        irrf= v - 0
        sf=v-irrf
        print (f' O valor do salário é {v}. O valor do imposto a pagar é {irrf}. Salário final é {sf}' )
else:  
        irrf = (v * 27) / 100 
        sf= v - irrf
        print (f' O valor do salário é {v}. O valor do imposto a pagar é {irrf}. Salário final é {sf}' )
        print('teste git')
        #####

